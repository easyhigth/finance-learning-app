// French translations for vocabulary fiches (vocab.js). Keyed by vocab id
// (e.g. "v-tvm"). Empty for now — localizeVocab falls back to the English
// source when an id is missing, so partial translations never break rendering.
// Fill in incrementally: { 'v-tvm': { term, simple, example, remember } }
export const vocabFR = {};