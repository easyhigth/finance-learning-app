// French translations for deep concepts (concepts.js). Keyed by concept id
// (e.g. "time-value-of-money"). Empty for now — localizeConcept falls back to
// the English source when an id is missing, so partial translations never
// break rendering.
// Fill in incrementally: { 'time-value-of-money': { title, hook, tldr, definition, keyPoints, ... } }
export const conceptsFR = {};