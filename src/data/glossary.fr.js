// French translations for glossary terms (glossary.js). Keyed by the English
// `term` string. Empty for now — localizeGlossary falls back to the English
// source when a term is missing, so partial translations never break rendering.
// Fill in incrementally: { 'Time Value of Money': { term, def } }
export const glossaryFR = {};